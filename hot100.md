# h100

## 1 哈希

哈希表可以在以 O(1) 的时间复杂度降低遍历的成本，Java 里我们需要熟悉一些简单的方法：

```java
// 如果不存在该 key，则执行表达式初始化，如果存在则返回 value
map.computeIfAbsent(key, (k) -> {});

// 如果不存在该 key，就用 value 初始化，如果存在，就对 value 执行函数，并返回处理后的 newValue，例如：map.merge(key, 1, Integer::sum);
map.merge(key, value, function);
```

### 1.1 两数之和

[1. 两数之和](https://leetcode.cn/problems/two-sum/)

#### 描述

> 给一个整数数组 nums 和一个整数目标值 target，请你在该数组找出**和为目标值** target 的两个整数，返回它们的数组下标。

#### 分析

处理数组问题，要考虑如何遍历，时间复杂度就是看几层循环，这道题最简单的思路是固定其中一个值，然后循环其他数，判断是否和为 target。当然这不符合我们的时间复杂度，实际上在一次遍历过程中，用哈希表存储并判断当前访问的元素 e 的差值 target-e 是否在哈希表存在就可以了。

#### 代码

```java
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int diff = target - nums[i];
        if (map.containsKey(diff)) {
            return new int[]{map.get(diff), i};
        }
        map.put(nums[i], i);
    }
    return new int[2];
}
```

### 1.2 字母异位词分组

[49. 字母异位词分组](https://leetcode.cn/problems/group-anagrams/)

#### 描述

> 给你一个字符串数组，请你将 **字母异位词** 组合在一起。可以按任意顺序返回结果列表。**字母异位词** 是由重新排列源单词的所有字母得到的一个新单词。
>
> 输入：strs = ["eat", "tea", "tan", "ate", "nat", "bat"] 
> 输出：[["bat"],["nat","tan"],["ate","eat","tea"]]

#### 分析

字母异位词是重新排列原单词得到的，所以如果对每一个字符串本身进行排序，如果是字母异位词，则它们是相同的。所以我们以排序完的字符串为 key，以相同字母异位词列表为 value，在遍历的过程中，逐步添加到对应 key 的 value 里即可。

#### 代码

```java
public List<List<String>> groupAnagrams(String[] strs) {
    Map<String, List<String>> map = new HashMap<>();
    for (String str : strs) {
        char[] cs = str.toCharArray();
        Arrays.sort(cs);
        String key = new String(cs);
        map.computeIfAbsent(key, k -> new ArrayList<>()).add(str);
    }
    return new ArrayList<>(map.values());
}
```

### 1.3 最长连续序列

[128. 最长连续序列](https://leetcode.cn/problems/longest-consecutive-sequence/)

#### 描述

> 给定一个未排序的整数数组`nums`，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。请你设计并实现时间复杂度为`O(n)`的算法解决此问题。
>
> 输入：nums = [100,4,200,1,3,2]
> 输出：解释：最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。

#### 分析

要找出数字连续的最长序列的长度，排序当然是最简单的方式，但不满足时间复杂度，那么我们得思路就是哈希表来降低时间复杂度，我们用哈希表存储整个整数数组，然后在遍历的过程中，判断连续的下一个数字是否存在，并在此过程记录一个最大长度即可。值的注意的是:

- 为了防止重复判断，当当前遍历的数 num - 1 在哈希表存在，说明已经处理完该序列，直接跳过即可。
- 因为数组可能有大量重复元素，所以当遍历的时候，遍历哈希表，不要遍历原数组

#### 代码

```java
public int longestConsecutive(int[] nums) {
	int n = nums.length;
    int maxLen = 0;
    Set<Integer> set = new HashSet<>();
    for (int num : nums) set.add(num);
    for (int num : set) { // !!!
        if (set.contains(num - 1)) continue;
        int len = 0;
        while (set.contains(num++)) len++;
        maxLen = Math.max(maxLen, len);
    }
    return maxLen;
}
```

## 2 双指针

双指针一般对应解决数组类的题目，那么 java 中，我们需要熟悉最简单的几个基本方法：

```java
// 指定值填充数组
Arrays.fill(nums, 1);

// 指定值填充数组[fromIdx, toIdx)范围
Arrays.fill(nums, fromIdx, toIdx, 1); 

// copy指定长度数组元素到目标数组
System.arraycopy(oriArr, startIdx, targetArr, targetIdx, len);

// 交换数组元素
private void exch(int[] arr, int i, int j) {
    int tmp = arr[i];
    arr[i] = arr[j];
    arr[j] = tmp;
}
```

### 2.1 移动零

[283. 移动零](https://leetcode.cn/problems/move-zeroes/)

#### 描述

> 给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时保持非零元素的相对顺序。注意必须在不复制数组的情况下原地对数组进行操作。

#### 分析

不让复制数组，然后要将非 0 元素按照相对顺序全都往前移动，那只能在原数组上进行操作。因为没有其他处理，我们只需要将非 0 元素放到前面，处理完后把最后一个非 0 元素后面的所有元素置零即可。所以我们可以用双指针来处理一次遍历中的赋值。

#### 代码

```java
public void moveZeroes(int[] nums) {
    int n = nums.length;
    int l = 0, r = 0;
    while (r < n) {
        if (nums[r] != 0) nums[l++] = nums[r];
        r++;
    }
    Arrays.fill(nums, l, n, 0);
    return;
}
```

### 2.2 盛最多水的容器

[盛最多水的容器](https://leetcode.cn/problems/container-with-most-water/)

#### 描述

> 给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。返回容器可以储存的最大水量。
>
> 输入：[1,8,6,2,5,4,8,3,7]
> 输出：49 

#### 分析

要寻找数组两条线所围成的面积最大的一片区域，我们可以遍历所有的组合，但是我们如何在一次遍历过程中就得到最终储存的最大水量呢？我们从两边往中间压缩，该区域的长是一直变小的，所以要得到更大的面积，我们需要找出**更大的宽**，然后一直判断下去直到两指针相遇。

#### 代码

```java
public int maxArea(int[] height) {
    int maxArea = 0;
	int n = height.length;
    int l = 0, r = n - 1;
    while (l < r) {
        int length = r - l;
        int width = Math.min(height[l], height[r]);
        maxArea = Math.max(maxArea, length * width);
        if (height[l] > height[r]) r--;
        else l++;
    }
    return maxArea;
}
```

### 2.3 三数之和

[15. 三数之和](https://leetcode.cn/problems/3sum/)

#### 描述

> 给你一个整数数组 `nums` ，判断是否存在三元组 `[nums[i], nums[j], nums[k]]` 满足 `i != j`、`i != k` 且 `j != k` ，同时还满足 `nums[i] + nums[j] + nums[k] == 0` 。请你返回所有和为 `0` 且不重复的三元组。
>
> **注意：**答案中不可以包含重复的三元组。

#### 分析

要找出所有和为 0 且不重复的三元组，找出这样的组合并不复杂，但是如何去重呢？为了避免找到重复的三元组，我们首先需要对原数组排序。然后要通过三次循环吗？既然排序了，我们完全不需要多次循环了，我们固定一个值，然后在该值后的首尾设立两个指针根据大小关系判断即可。

#### 代码

```java
public List<List<Integer>> threeSum(int[] nums) {
    int n = nums.length;
    Arrays.sort(nums);
    List<List<Integer>> ans = new ArrayList<>();
    for (int i = 0; i < n - 2; i++) {
        if (i > 0 && nums[i] == nums[i - 1]) continue; // 外层去重
        int l = i + 1, r = n - 1;
        int diff = -nums[i];
        while (l < r) {
            int sum = nums[l] + nums[r];
            if (sum == diff) {
                ans.add(new ArrayList<>(Arrays.asList(nums[i], nums[l], nums[r])));
                l++;
                r--;
                while (l < r && nums[l] == nums[l - 1]) l++; // 内层去重
                while (r > l && nums[r] == nums[r + 1]) r--; // 内层去重
            }
            else if (sum > diff) r--;
            else l++;
        }
    }
    return ans;
}
```

### 2.4 接雨水

[42. 接雨水](https://leetcode.cn/problems/trapping-rain-water/)

#### 描述

> 给定 `n` 个非负整数表示每个宽度为 `1` 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。

#### 分析

典型题目，典中典，这道题目和盛最多水的容器那道题目很类似，最多水的容器实质上可以将柱子看为一条不占面积的线，从而求围最大面积的两条线。接雨水的柱子是有面积的，是 1 的单位长度，接水面积是通过两侧的柱子自身的高度包围出来的。那么我们需要每一个高度的柱子所能接水的面积由什么决定？先确定思路很关键，很明显当前高度的柱子所能装的水是由左侧最高和右侧最高的较低值，减去当前高度决定的。基于这个思路就可以通过提前准备好两个数组，代表当前柱子左侧最高和右侧最高的高度，然后遍历一次即可。

提前处理 lm 和 rm 数组其实可以通过单调栈一次遍历实现，但**单调栈**不是以某一个柱子接的雨水求和的，而是当前柱子和栈顶柱子所围的面积求和。

#### 代码 1

```java
public int trap(int[] height) {
    int ans = 0;
    int n = height.length;
    int[] lm = new int[n]; // 当前柱子左侧最大值
    int[] rm = new int[n]; // 当前柱子右侧最大值
    for (int i = 1; i < n; i++) lm[i] = Math.max(lm[i - 1], height[i - 1]);
    for (int i = n - 2; i >= 0; i--) rm[i] = Math.max(rm[i + 1], height[i + 1]);

    for (int i = 0; i < n; i++) {
        int width = Math.min(lm[i], rm[i]);
        if (width < height[i]) continue;
        ans += width - height[i];
    }
    return ans;
}
```

#### 代码 2

```java
// 提前处理 lm 和 rm 数组其实可以通过单调栈一次遍历实现，但单调栈不是以某一个柱子接的雨水求和的
public int trap(int[] height) {
    int ans = 0;
    int n = height.length;
    Deque<Integer> monoStack = new ArrayDeque<>();
    for (int i = 0; i < n; i++) {
        while (!monoStack.isEmpty() && height[monoStack.peek()] < height[i]) {
            int idx = monoStack.pop();
            if (monoStack.isEmpty()) break;
            int lIdx = monoStack.peek();
            int width = Math.min(height[i], height[lIdx]) - height[idx];
            ans += width * (i - lIdx - 1);
        }
        monoStack.push(i);
    }
    return ans;
}
```

#### 代码 3

我们通过记录 lm 和 rm 数组来求解当前柱子能存的雨水，但是实际上我们取的是较小值，什么意思呢，我们从数组两边用 lm 和 rm 两个变量来遍历，表示 l 指针左侧的最大值和 r 指针右侧的最大值，当我们处理 l 或者 r 的时候，因为我们取得是两侧更大值的较小值，所以如果 lm < rm，那 l->r 指针中间的更大的值都没有意义了，我们已经能计算 l 处装的雨水 lm - height[l]；如果 rm < lm 则 l->  r 中间的更大值也没有意义了，因为我们已经获得最大值里的较小值。因为已经和 lm 和 rm 比较了，所以一侧的最大值只能比 lm 大，不然比较没意义。

```java
public int trap(int[] height) {
    int ans = 0;
    int n = height.length;
    int lm = height[0], rm = height[n - 1];
    int l = 0, r = n - 1;
    while (l < r) {
        lm = Math.max(lm, height[l]);
        rm = Math.max(rm, height[r]);
        if (lm < rm) ans += lm - height[l++];
        else ans += rm - height[r--];
    }
    return ans;
}
```



## 3 滑动窗口

滑动窗口可以在一次遍历中解决最优化问题，先让窗口的右指针走到题目要求边界范围，然后在 while 收缩左窗口边界，在此过程，一直更新结果即可。

### 3.1 无重复字符的最长子串

[3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

#### 描述

> 给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。
>
> ```
> 输入: s = "abcabcbb"
> 输出: 3 
> 解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
> ```

#### 分析

暴力解法就是判断所有子串，然后记录不重复子串的最大长度，但是这个过程中，我们总是维持窗口是无重复字符的子串，保证窗口的性质即可。

#### 代码

```java
public int lengthOfLongestSubstring(String s) {
	char[] cs = s.toCharArray();
    int ans = 0;
    int l = 0;
    int[] cnt = new int[128];
    for (int i = 0; i < cs.length; i++) {
      	char c = cs[i];
        cnt[c]++;
        while (cnt[c] > 1) {
            cnt[cs[l]]--;
            l++;
        }
        ans = Math.max(ans, i - l + 1);
    }
    return ans;
}
```

### 3.2 找到字符串中所有字母异位词

[438. 找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/)

#### 描述

> 给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 **异位词** 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。
>
> ```
> 输入: s = "cbaebabacd", p = "abc"
> 输出: [0,6]
> ```

#### 分析

异位词就是相同的字母组成只是顺序不一样，那要在 s 中找到所有异位词的起始索引，我们可以固定长度为一个窗口，然后平移窗口从开始到最后即可，在这个过程中记录结果。

#### 代码

```java
public List<Integer> findAnagrams(String s, String p) {
    List<Integer> ans = new ArrayList<>();
	int pLen = p.length();
    char[] cs = s.toCharArray();
    int n = cs.length;
    Map<Character, Integer> pmap = new HashMap<>();
    for (char c : p.toCharArray()) map.merge(c, 1, Integer::sum);
    Map<Character, Integer> smap = new HashMap<>();
    int l = 0;
    for (int i = 0; i < n - subLen; i++) {
        char c = cs[i];
        map.merge(c, 1, Integer::sum);
        if (i - l + 1 == pLen) {
            if (check(smap, pmap)) {
                ans.add(l);
            } else {
                smap.merge(cs[l], -1, Integer::sum);
            }
            l++;
        }
    }
    return ans;
}

private boolean check(Map<Character, Integer> smap, Map<Character, Integer> pmap) {
    if (smap.size() != pmap.size()) return false;
    for (Map.Entry<Character, Integer> entry : pmap) {
        if (!smap.containsKey(entery.getKey())) return false;
        else if (!Objects.equals(smap.get(entry.getKey()), entry.getValue())) return false; // Integer 的判断不要用 !=
    }
    return true;
}
```

## 4 子串

记住 **子串和子数组是连续的**。

### 4.1 和为 K 的子数组

[和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/)

#### 描述

> 给你一个整数数组 `nums` 和一个整数 `k` ，请你统计并返回 *该数组中和为 `k` 的子数组的个数* 。
>
> ```
> 输入：nums = [1,1,1], k = 2
> 输出：2
> ```

#### 分析

子串子数组都有连续性。再谨记： **数组不是单调的话，不要用滑动窗口，因为左窗口会向左扩展，考虑用前缀和**。$SUM(nums[left]...nums[right]) = preSum[right+1] - preSum[left]$

[前缀和解析](https://leetcode.cn/problems/range-sum-query-immutable/solutions/2693498/qian-zhui-he-ji-qi-kuo-zhan-fu-ti-dan-py-vaar/)

当把数组里所有子数组的和的形式以前缀和转变为两数之和的形式，就可以按照两数之和的思想来做这道题了，无非是遍历一个新值并将新值放入哈希表的过程中，看是否旧值出现过。

#### 代码

```java
public int subarraySum(int[] nums, int k) {
    int n = nums.length;
    int ans = 0;
    int[] preSum = new int[n + 1];
    for (int i = 0; i < n; i++) {
        preSum[i + 1] = preSum[i] + nums[i];
    }
    Map<Integer, Integer> map = new HashMap<>(n + 1);
    for (int i = 0; i < n + 1; i++) { // 不能忘掉第一个值的前缀和
        ans += map.getOrDefault(preSum[i] - k, 0);
        map.merge(preSum[i], 1, Integer::sum);
    }
    return ans;
}
```

### 4.2 滑动窗口最大值

[239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)

#### 描述

> 给你一个整数数组 `nums`，有一个大小为 `k` 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 `k` 个数字。滑动窗口每次只向右移动一位。返回 *滑动窗口中的最大值* 。
>
> ```
> 输入：nums = [1,3,-1,-3,5,3,6,7], k = 3
> 输出：[3,3,5,5,6,7]
> ```

#### 分析

我们要维护一个窗口，该窗口始终返回当前遍历的三个值的最大值，我们维护一个单调队列，最大栈，不能用堆，因为堆能保证返回最大值，但不能保证当前值是不是该窗口内的。

#### 代码 1

```java
public int[] maxSlidingWindow(int[] nums, int k) {
    int n = nums.length;
    Deque<Integer> monoMaxQ = new LinkedList<>(); // monotonic max queue
    int[] ans = new int[n - k + 1];
    for (int i = 0; i < nums.length; i++) {
        while (!monoMaxQ.isEmpty() && monoMaxQ.peekLast() < nums[i]) {
            monoMaxQ.pollLast();
        }
        monoMaxQ.offerLast(nums[i]);
        if (i >= k - 1) {
            ans[i-k+1] = monoMaxQ.peekFirst();
            if (!monoMaxQ.isEmpty() && nums[i - k + 1] == monoMaxQ.peekFirst()) {
                monoMaxQ.pollFirst();
            }
        }
    }
    return ans;
}
```

#### 代码 2

我们可以直接用索引来判断最大元素是否已经出了窗口，将**数组下标**入队列。

```java
public int[] maxSlidingWindow(int[] nums, int k) {
    int n = nums.length;
    Deque<Integer> monoMaxQ = new LinkedList<>(); // monotonic max queue
    int[] ans = new int[n - k + 1];
    for (int i = 0; i < nums.length; i++) {
        while (!monoMaxQ.isEmpty() && nums[monoMaxQ.peekLast()] < nums[i]) {
            monoMaxQ.pollLast();
        }
        monoMaxQ.offerLast(i);

        if (i - monoMaxQ.peekFirst() >= k) {
            monoMaxQ.pollFirst();
        }
        if (i >= k - 1) {
            ans[i - k + 1] = nums[monoMaxQ.peekFirst()];
        }
    }
    return ans;
}
```

### 4.3 最小覆盖子串

[76. 最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring/)

#### 描述

> 给你一个字符串 `s` 、一个字符串 `t` 。返回 `s` 中涵盖 `t` 所有字符的最小子串。如果 `s` 中不存在涵盖 `t` 所有字符的子串，则返回空字符串 `""` 。
>
> ```
> 输入：s = "ADOBECODEBANC", t = "ABC"
> 输出："BANC"
> 解释：最小覆盖子串 "BANC" 包含来自字符串 t 的 'A'、'B' 和 'C'。
> ```

#### 分析

用两个 map 去记录遍历到的 s 的子串和 t 中所有字符及对应的出现次数，当 s 的子串包含 t 串后（两个 map 的包含关系），开始以滑动窗口的想法从左到右缩减 s 子串并记录更新结果。

#### 代码

```java
public String minWindow(String s, String t) {
    Map<Character, Integer> tMap = new HashMap<>();
    for (int i = 0; i < t.length(); i++) {
        tMap.merge(t.charAt(i), 1, Integer::sum);
    }
    Map<Character, Integer> sMap = new HashMap<>();
    int r = 0;
    int l = 0;
    int minLen = Integer.MAX_VALUE;
    String res = "";
    while (r < s.length()) {
        sMap.merge(s.charAt(r), 1, Integer::sum);
        while (l <= r && check(sMap, tMap)) {
            if (r - l + 1 < minLen) {
                minLen = r - l + 1;
                res = s.substring(l, r+1);
            }
            sMap.merge(s.charAt(l), -1, Integer::sum);
            l++;
        }
        r++;
    }
    return res;
}

private boolean check(Map<Character, Integer> sMap, Map<Character, Integer> tMap) {
    Set<Map.Entry<Character, Integer>> entrySet = tMap.entrySet();
    for (Map.Entry<Character, Integer> entry : entrySet) {
        if (!sMap.containsKey(entry.getKey())) return false;
        if (sMap.get(entry.getKey()) < entry.getValue()) return false;
    }
    return true;
}
```

## 5 普通数组

Java 里数组的预定义方法比较局限，最基本的 swap 和 reverse 都需要自己实现。

```java
private void exch(int[] nums, int i, int j) {    
    int aux = nums[i];
    nums[i] = nums[j];
    nums[j] = aux;
}
private void reverse(int[] nums, int i, int j) {
    while (i < j) {
        int aux = nums[i];
        nums[i] = nums[j];
        nums[j] = aux;
        i++;
        j--;
    }
}
```



### 5.1 最大子数组和

[53. 最大子数组和](https://leetcode.cn/problems/maximum-subarray/)

#### 描述

> 给你一个整数数组 `nums` ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。
>
> ```
> 输入：nums = [-2,1,-3,4,-1,2,1,-5,4]
> 输出：6
> 解释：连续子数组 [4,-1,2,1] 的和最大，为 6 。
> ```

#### 分析

最简单的两层循环遍历所有可能，记录最大值，时间复杂度当然不满足要求，因为我们每次都是重新求的子数组和，没有用到后一个子数组和可以在前一个和的基础上增加该元素值就得到的数组隐藏的线索。如果全局分析没有思路，我们就需要按照数组的遍历顺序，从左往右去分析，处理的负值会让原子数组和变小，正值会使子数组和增大。所以考虑以 nums[i] 元素结尾的子数组的和的来源，如果要借助到之前元素的子数组和，我们可以推出：

- 如果 subArraySum[i - 1] > 0，则会让包含 nums[i] 元素的子数组值变大
- 如果 subArraySum[i - 1] < 0，则我们只需要让 nums[i] 单独成为一个子数组即可

#### 代码

```java
public int maxSubArray(int[] nums) {
    int n = nums.length;
    // dp[i] 表示以 nums[i] 结尾的最大子数组和
    int[] dp = new int[n];
    dp[0] = nums[0];
    int ans = dp[0];
    for (int i = 1; i < n; i++) {
        if (dp[i - 1] > 0) dp[i] = dp[i - 1] + nums[i];
        else dp[i] = nums[i];
        ans = Math.max(dp[i], ans);
    }
    return ans;
}
```

### 5.2 合并区间

[56. 合并区间](https://leetcode.cn/problems/merge-intervals/)

#### 描述

> 以数组 `intervals` 表示若干个区间的集合，其中单个区间为 `intervals[i] = [starti, endi]` 。请你合并所有重叠的区间，并返回 *一个不重叠的区间数组，该数组需恰好覆盖输入中的所有区间* 。
>
> ```
> 输入：intervals = [[1,3],[2,6],[8,10],[15,18]]
> 输出：[[1,6],[8,10],[15,18]]
> 解释：区间 [1,3] 和 [2,6] 重叠, 将它们合并为 [1,6].
> ```

#### 分析

要合并重叠区间，第一件事肯定是排序，那根据区间的左边界还是右边界来排序呢？毫无疑问，左边，排序后我们遍历的过程中需要记录合并后的区间的左右边界，左边界肯定是上一个的左边界，右边界根据比对关系决定更大的右边界，然后更新结果里的上一个区间的有边界即可。

#### 代码

```java
public int[][] merge(int[][] intervals) {
    int n = intervals.length;
    Arrays.sort(intervals, (f, s) -> Integer.compare(f[0], s[0]));
    List<int[]> ans = new ArrayList<>();
    for (int[] interval : intervals) {
        if (ans.size() == 0) ans.add(new int[]{interval[0], interval[1]});
        else {
            int[] preInterval = ans.get(ans.size() - 1);
            if (interval[0] > preInterval[1]) ans.add(new int[]{interval[0], interval[1]});
            else preInterval[1] = Math.max(preInterval[1], interval[1]);
        }
    }
    return ans.toArray(new int[ans.size()][0]);
}
```

### 5.3 轮转数组

[189. 轮转数组](https://leetcode.cn/problems/rotate-array/)

#### 描述

> 给定一个整数数组 `nums`，将数组中的元素向右轮转 `k` 个位置，其中 `k` 是非负数。
>
> ```
> 输入: nums = [1,2,3,4,5,6,7], k = 3
> 输出: [5,6,7,1,2,3,4]
> ```

#### 分析

数组的解决方式有很多，但是最好的方式是原地解决，就是不需要新的空间复杂度开销。主要是要发现规律，我们可以发现长度为 n 的数组轮转 n 次刚好复原，所以不管 k 是多少，反转后的结果和 k % n 如何不开辟新的空间实现元素的移动呢？这里就需要记住一个新的思路了，**多次反转**，但是我们发现普通的反转并不能完成题目要求，那就分段多次反转，和链表的思想有点相似，分两部分分别处理再合并。

#### 代码

```java
public void rotate(int[] nums, int k) {
    int n = nums.length;
    k = k % n;
    reverse(nums, 0, n - 1);
    reverse(nums, 0, k - 1);
    reverse(nums, k, n - 1);
}
private void reverse(int[] nums, int l, int r) {
    while (l < r) {
        int aux = nums[l];
        nums[l] = nums[r];
        nums[r] = aux;
        l++;
        r--;
    }
}
```

### 5.4 除自身以外数组的乘积

[238. 除自身以外数组的乘积](https://leetcode.cn/problems/product-of-array-except-self/)

#### 描述

>  给你一个整数数组 `nums`，返回 数组 `answer` ，其中 `answer[i]` 等于 `nums` 中除 `nums[i]` 之外其余各元素的乘积 。题目数据 **保证** 数组 `nums`之中任意元素的全部前缀元素和后缀的乘积都在 **32 位** 整数范围内。请 **不要使用除法，**且在 `O(n)` 时间复杂度内完成此题。
>
> ```
> 输入: nums = [-1,1,0,-3,3]
> 输出: [0,0,9,0,0]
> ```

#### 分析

从前缀和后缀的乘积就能看出来，我们需要用动态规划避免重复计算。

#### 代码

```java
public int[] productExceptSelf(int[] nums) {
    int n = nums.length;
    int[] ans = new int[n];
    ans[0] = 1;
    for (int i = 1; i < n; i++) {
        ans[i] = ans[i - 1] * nums[i - 1];
    }
    int suffix = 1; // 因为只用一次，直接用 suffix 表示
    for (int i = n - 1; i >= 0; i--) {
        ans[i] = ans[i] * suffix;
        suffix = suffix * nums[i];
    }
    return ans;
}
```

### 5.5 缺失的第一个正数

[41. 缺失的第一个正数](https://leetcode.cn/problems/first-missing-positive/)

#### 描述

> 给你一个未排序的整数数组 `nums` ，请你找出其中没有出现的最小的正整数。请你实现时间复杂度为 `O(n)` 并且只使用常数级别额外空间的解决方案。
>
> ```
> 输入：nums = [3,4,-1,1]
> 输出：2
> 解释：1 在数组中，但 2 没有。
> ```

#### 分析

最简单的做法是定义一个 hashmap 存储原数组所有元素，然后从 1->n 判断是否存在，不存在即找到一个最小的正整数。但是这样的空间复杂度是 O(n)，那要常数的空间复杂度还有什么思考方向呢？要常数的空间，意味着我们必须依赖于原数组存储 hash 对应关系，即***原地哈希***，用 `hash(nums[i] - 1) = nums[i]` 这样的哈希算法一直交换原数组元素直到它满足题目要求。

#### 代码

```java
public int firstMissingPositive(int[] nums) {
	int n = nums.length;
    for (int i = 0; i < n; i++) {
        while (nums[i] > 0 && nums[i] < n && nums[i] != nums[nums[i] - 1]) exch(nums, i, nums[i] - 1);
    }
    int v = 1;
    while (v <= n && nums[v - 1] == v) v++;
    return v;
}
private void exch(int[] nums, int i, int j) {
    int aux = nums[i];
    nums[i] = nums[j];
    nums[j] = aux;
}
```

## 6 矩阵

### 6.1 矩阵置零

[73. 矩阵置零](https://leetcode.cn/problems/set-matrix-zeroes/)

#### 描述

> 给定一个 m x n 的矩阵，如果一个元素为 **0** ，则将其所在行和列的所有元素都设为 **0** 。请使用 **[原地](http://baike.baidu.com/item/原地算法)** 算法**。****进阶：**
>
> - 一个直观的解决方案是使用  `O(m*n)` 的额外空间，但这并不是一个好的解决方案。
> - 一个简单的改进方案是使用 `O(m + n)` 的额外空间，但这仍然不是最好的解决方案。
> - 你能想出一个仅使用常量空间的解决方案吗？
>
> 
>
> **示例 1：**
>
> <img src="https://assets.leetcode.com/uploads/2020/08/17/mat1.jpg" alt="img" style="zoom:50%;" />
>
> ```
> 输入：matrix = [[1,1,1],[1,0,1],[1,1,1]]
> 输出：[[1,0,1],[0,0,0],[1,0,1]]
> ```

#### 分析

容易想到的就是记录 m 行和 n 列是否存在值的信息，第一次遍历记录，第二次遍历来根据记录的信息决定是否置为 0，但是这样的空间复杂度是 O(m + n)，题目要求最好常量的空间复杂度解决问题，那我们必须利用到原数组来存储我们遍历过记录的信息。那解决了怎么存的问题，也就是解决了常量空间的优化。

#### 代码 1

```java
public void setZeroes(int[][] matrix) {
    int m = matrix.length;
    int n = m == 0 ? 0 : matrix[0].length;
    boolean[] row = new boolean[m];
    boolean[] col = new boolean[n];
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (matrix[i][j] == 0) {
                row[i] = true;
                col[j] = true;
            }
        }
    }
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (row[i] || col[j]) matrix[i][j] = 0;
        }
    }
    return;
}
```

#### 代码 2

```java
public void setZeroes(int[][] mat) {
    int m = mat.length, n = mat[0].length;
    // 1. 扫描「首行」和「首列」记录「首行」和「首列」是否该被置零
    boolean r0 = false, c0 = false;
    for (int i = 0; i < m; i++) {
        if (mat[i][0] == 0) {
            r0 = true;
            break;
        }
    }
    for (int j = 0; j < n; j++) {
        if (mat[0][j] == 0) {
            c0 = true;
            break;
        }
    }
    // 2.1 扫描「非首行首列」的位置，如果发现零，将需要置零的信息存储到该行的「最左方」和「最上方」的格子内
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            if (mat[i][j] == 0) mat[i][0] = mat[0][j] = 0;
        }
    }
    // 2.2 根据刚刚记录在「最左方」和「最上方」格子内的置零信息，进行「非首行首列」置零
    for (int j = 1; j < n; j++) {
        if (mat[0][j] == 0) {
            for (int i = 1; i < m; i++) mat[i][j] = 0;
        }
    }
    for (int i = 1; i < m; i++) {
        if (mat[i][0] == 0) Arrays.fill(mat[i], 0);
    }
    // 3. 根据最开始记录的「首行」和「首列」信息，进行「首行首列」置零
    if (r0) for (int i = 0; i < m; i++) mat[i][0] = 0;
    if (c0) Arrays.fill(mat[0], 0);
    return;
}
```

### 6.2 螺旋矩阵

[54. 螺旋矩阵](https://leetcode.cn/problems/spiral-matrix/)

#### 描述

> 给你一个 `m` 行 `n` 列的矩阵 `matrix` ，请按照 **顺时针螺旋顺序** ，返回矩阵中的所有元素。**示例 1：**
>
> <img src="https://assets.leetcode.com/uploads/2020/11/13/spiral1.jpg" alt="img" style="zoom:50%;" />
>
> ```
> 输入：matrix = [[1,2,3],[4,5,6],[7,8,9]]
> 输出：[1,2,3,6,9,8,7,4,5]
> ```

#### 分析

要顺时针遍历所有元素，我们首先得知道遍历到什么条件下，遍历结束？其实就是处理了 m * n 个元素截止。那如何遍历呢，我们需要处理向右、向下、向左、向上这四条路径，并且循环，所以需要有变量去记录这四个路径的范围，我们用 r、b、l、t 来表示。

为了避免遍历完所有元素，左->右，右->左....这种循环遍历导致可能添加多余元素的问题，需要处理完一层后判断是否遍历完毕。

#### 代码

```java
public List<Integer> spiralOrder(int[][] matrix) {
    List<Integer> ans = new ArrayList<>();
    int m = matrix.length;
    int n = m == 0 ? 0 : matrix[0].length;
    int size = m * n;
    int l = 0, r = n - 1, t = 0, b = m - 1;
    while (ans.size() < size) {
        if (ans.size() == m * n) break;
        for (int i = l; i <= r; i++) ans.add(matrix[t][i]);
        t++;
        if (ans.size() == m * n) break;
        for (int i = t; i <= b; i++) ans.add(matrix[i][r]);
        r--;
        if (ans.size() == m * n) break;
        for (int i = r; i >= l; i--) ans.add(matrix[b][i]);
        b--;
        if (ans.size() == m * n) break;
        for (int i = b; i >= t; i--) ans.add(matrix[i][l]);
        l++;
    }
    return ans;
}
```

### 6.3 旋转图像

[48. 旋转图像](https://leetcode.cn/problems/rotate-image/)

#### 描述

> 给定一个 *n* × *n* 的二维矩阵 `matrix` 表示一个图像。请你将图像顺时针旋转 90 度。
>
> 你必须在**[ 原地](https://baike.baidu.com/item/原地算法)** 旋转图像，这意味着你需要直接修改输入的二维矩阵。**请不要** 使用另一个矩阵来旋转图像。
>
> **示例 1：**
>
> <img src="https://assets.leetcode.com/uploads/2020/08/28/mat1.jpg" alt="img" style="zoom:50%;" />
>
> ```
> 输入：matrix = [[1,2,3],[4,5,6],[7,8,9]]
> 输出：[[7,4,1],[8,5,2],[9,6,3]]
> ```

#### 分析

直接从原图像去分析解决方案，发现想要原地旋转很困难，几乎没有思路，遇到困难的问题，就需要发散思维，可能需要先将原状态处理成另一个状态再进行思考，可能需要从不同的方向和角度进行思考。这道题既然要原地，那么我们能用的的手段只有 exch，那么如何交换，直接交换完能不能得到最终结果？答案是否定的，交换为一个新状态是第一步骤，在交换完后的状态，我们就能很清晰地看到解题思路。

我们沿着 135 度斜线翻转原数组，得到新状态数组，再对新状态进行每一行的翻转，就得到了目标数组。

```
1 2 3	 1 4 7     7 4 1
4 5 6 => 2 5 8  => 8 5 2 
7 8 9    3 8 9     9 8 3
```

#### 代码

```java
public void rotate(int[][] matrix) {
	int n = matrix.length;
    for (int i = 0; i < n; i++) {
        for (int j = i; j < n; j++) { // 遍历一半
            exch(matrix, i, j);
        }
    }
    for (int[] m : matrix) reverse(m, 0, m.length - 1);
}

private void exch(int[][] matrix, int i, int j) {
    int aux = matrix[i][j];
    matrix[i][j] = matrix[j][i];
    matrix[j][i] = aux;
}

private void reverse(int[] nums, int i, int j) {
    while (i < j) {
        int aux = nums[i];
        nums[i] = nums[j];
        nums[j] = aux;
        i++;
        j--;
    }
}
```

### 6.4 搜索二维矩阵 II

[240. 搜索二维矩阵 II](https://leetcode.cn/problems/search-a-2d-matrix-ii/)

#### 描述

> 编写一个高效的算法来搜索 `*m* x *n*` 矩阵 `matrix` 中的一个目标值 `target` 。该矩阵具有以下特性：
>
> - 每行的元素从左到右升序排列。
> - 每列的元素从上到下升序排列。
>
> **示例 1：**
>
> <img src="https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2020/11/25/searchgrid2.jpg" alt="img" style="zoom:50%;" />
>
> ```
> 输入：matrix = [[1,4,7,11,15],[2,5,8,12,19],[3,6,9,16,22],[10,13,14,17,24],[18,21,23,26,30]], target = 5
> 输出：true
> ```

#### 分析

矩阵本身是有不同的排序规则，所以如果按照常规的遍历思路是不好去找遍历方式的，我们没必要把思路放在常规的 (0,0) -> (n,n)，而是可以根据目前的矩阵的特点重新设计我们的遍历逻辑遍历方向。

#### 代码

```java
public boolean searchMatrix(int[][] matrix, int target) {
	int m = matrix.length;
    int n = m == 0 ? 0 : matrix[0].length;
    int i = m - 1, j = 0;
    while (i >= 0 && j < n) {
        if (matrix[i][j] == target) return true;
        else if (matrix[i][j] < target) j++;
        else i--;
    }
    return false;
}
```

## 7 链表

### 7.1 相交链表

[160. 相交链表](https://leetcode.cn/problems/intersection-of-two-linked-lists/)

#### 描述

>  

#### 分析

#### 代码